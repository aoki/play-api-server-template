# Routes
# This file defines all application routes (Higher priority routes first)

GET     /                               com.ringohub.controllers.Application.index(path = "/")
GET     /$path<.+>                      com.ringohub.controllers.Application.index(path)
